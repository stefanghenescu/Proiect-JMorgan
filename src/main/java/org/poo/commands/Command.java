package org.poo.commands;

public interface Command {
    /**
     * Executes the command. This method is implemented by every command class.
     * It is part of the Command design pattern.
     */
    void execute();
}
